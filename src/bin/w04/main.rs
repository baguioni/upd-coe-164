use std::collections::BinaryHeap;
use std::ops::Deref;
use std::io;

#[derive(Debug, Ord, PartialOrd, PartialEq, Eq)]
struct Request<T> (T);

impl <T> Deref for Request <T> {
    // TODO
}

fn main(){
    // TODO
}